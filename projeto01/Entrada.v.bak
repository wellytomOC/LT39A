module Entrada (
    input wire clock
    input wire[2:0] col,

    output wire clock50Mhz, clock1Hz, clock500Hz,
    output wire[3:0] row, KeypadData,
    output wire dav
);

keypadEncoder2 inst01(clock500Hz, col, row, KeypadData, dav);
    
endmodule